services:
  nfc:
    build: .
    ports:
      - "8088:5000"
    volumes:
      - ./instance:/app/instance
      - ./uploads:/app/uploads
    env_file: .env
    environment:
      - TZ=Europe/Madrid
    restart: unless-stopped
    mem_limit: 512m
